# API-{ID}: {API名} 単体テスト仕様書

> 対象APIの詳細設計は [`api-{ID}-{slug}.md`](../3_detail-design/api/api-{ID}-{slug}.md)。テスト方針
> 全体は [`cross-cutting.md`](../_shared/cross-cutting.md)「テスト戦略」を正本とする。本書はAPI-{ID}の
> **ユニットテスト**（実DB不要）のケースのみを列挙する。実PostgreSQLを要する結合テストのケースは
> `docs/specs/5_integration-test/`（画面単位。`specs-integration-test` スキル）に書く
> （本書に結合テストのケースを混在させない）。

## テスト対象

| レイヤー | 対象 | テスト種別 |
|---|---|---|
| {Domain/Service/Handler} | `{関数・メソッド名}` | ユニットテスト |

## テストケース一覧

| No | 観点 | 前提・入力 | 期待結果 | 完了 |
|---|---|---|---|:--:|
| 1 | 正常系 | {具体的な入力値} | {具体的な期待結果} | [ ] |
| 2 | 境界値 | {値域の端} | {期待結果} | [ ] |
| 3 | 異常系 | {不正な入力・存在しないリソース} | {エラーステータス・メッセージ} | [ ] |

## 備考

- {テストの実装方針・依存する中核ロジックへの参照等}
- 実DB（Repository層の永続化・制約検証）を要するケースは本書に含めない。当該APIを使う画面の
  結合テスト仕様書（`docs/specs/5_integration-test/test-integration-{screen-slug}.md`）に
  含める。ケース番号は元の通し番号を維持し、互いのファイルへの参照リンクを付ける。
