# specs/

`docs/baseline/` の6ドキュメント（指標。常に参照する）には含まれない、各フェーズ（要件定義・基本設計・
詳細設計など）で参照・作成すべき成果物を置く場所。`baseline/` の工程分類（`CLAUDE.md` 参照）に対応する
番号付きディレクトリを初期配置している。

```
docs/specs/
├── 1_requirements/    # 要件定義フェーズの成果物
├── 2_basic-design/    # 基本設計フェーズの成果物
└── 3_detail-design/   # 詳細設計フェーズの成果物
```

現時点では各ディレクトリは未使用。今後の運用でこのディレクトリの使い方が固まり次第、内容を追記する。
